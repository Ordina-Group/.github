{
  "$schema": "https://docs.renovatebot.com/renovate-schema.json",
  "description": [
    "Base preset to configure automerge with pull requests and squash merges",
    "Should be combined with `:automergePatch`, `:automergeMinor` or `:automergeAll`, depending on project maturity level"
  ],
  "extends": [
    ":automergePr",
    "schedule:automergeNonOfficeHours"
  ],
  "automergeStrategy": "squash"
}
