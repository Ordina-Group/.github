{
  "$schema": "https://docs.renovatebot.com/renovate-schema.json",
  "description": ["Intended for workflows/actions as an action update requires a version bump and changelog registration"],
  "packageRules": [
    {
      "description": "Treat action as fix",
      "matchDepTypes": [
        "action"
      ],
      "semanticCommitType": "fix"
    }
  ]
}
